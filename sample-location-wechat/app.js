// @mini-dev/location 微信小程序示例
// 库通过 `require` 消费（CJS），与微信「构建 npm」产物一致。
// 平台由独立包选定（@mini-dev/location-wx），不探测；import 不会抛错。
App({
    onLaunch() {
        console.log('[App].onLaunch @mini-dev/location sample (wechat)');
    }
});
