// @mini-dev/location 支付宝小程序示例
// 库通过 `import` 消费（支付宝 IDE 开启 transpile 后支持 ESM 语法）。
// 平台由独立包选定（@mini-dev/location-my），不探测；import 不会抛错。
App({
    onLaunch() {
        console.log('[App].onLaunch @mini-dev/location sample (alipay)');
    }
});
