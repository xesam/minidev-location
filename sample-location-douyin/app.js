// @mini-dev/location 抖音小程序示例
// 库通过 `import` 消费（抖音 IDE 开启 es6 编译后支持 ESM 语法）。
// 平台由独立包选定（@mini-dev/location-tt），不探测；import 不会抛错。
App({
    onLaunch() {
        console.log('[App].onLaunch @mini-dev/location sample (douyin)');
    }
});
